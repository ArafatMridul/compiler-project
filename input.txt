#=><stdio.h>

num add(num a, num b) {
    give_back a + b;
}

Start_From_Here() {
    nmu wrongType <- 10;

    num total <- 5;
    ttoal <- add(total, 1);

    num arr[5] <- [1, 3, 5, 7, 9];
    num idx <- binary_serch(arr, 7);

    console.out("%d %d\n", total, idx);
}
